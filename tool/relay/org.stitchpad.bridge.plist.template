<?xml version="1.0" encoding="UTF-8"?>
<!-- stitchpad bridge — auto-starts at login, scopes discovery to one workspace root,
     mirrors each pad under that root to the relay, and
     drains phone→pad messages. KeepAlive restarts it if it dies.
     Install: `stitchpad bridge install` (fills paths/credentials and loads it). -->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>org.stitchpad.bridge</string>
  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>__HOME__/.stitchpad/relay/bridge.sh</string>
  </array>
  <key>EnvironmentVariables</key>
  <dict>
    <key>STITCHPAD_RELAY</key>
    <string>__RELAY__</string>
    <key>STITCHPAD_TOKEN</key>
    <string>__TOKEN__</string>
    <key>STITCHPAD_WORKSPACE_ROOT</key>
    <string>__WORKSPACE_ROOT__</string>
    <key>PATH</key>
    <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
  </dict>
  <key>RunAtLoad</key>
  <true/>
  <key>KeepAlive</key>
  <true/>
  <key>StandardOutPath</key>
  <string>__HOME__/.stitchpad/.bridge.log</string>
  <key>StandardErrorPath</key>
  <string>__HOME__/.stitchpad/.bridge.log</string>
</dict>
</plist>
