# suite-quarantine.txt — suites excluded from automated tripwire enforcement.
# Format: <suite-name> <one-line reason>
# Quarantined suites are SKIPPED with a loud warning. They still appear in
# the tripwire report so reviewers know they exist. A quarantine is NOT
# a free pass — it documents a measurement gap that must be justified.
# Additions to this file require a commit message explaining WHY the suite
# cannot be reliably measured.
help-discoverability-gate.sh requires: terminal-width (~80 cols) for help output assertions
pad-narration-gate.sh DEFERRED capability (P19 self-narrating pad) — designed 2026-08-04, not complete; see PR scorecard
oversight-gate.sh DEFERRED capability (P20/P21 ETA + live board) — designed 2026-08-04, not complete; see PR scorecard
operator-conduct-gate.sh DEFERRED capability (P22 operator conducting) — designed 2026-08-04, not complete; see PR scorecard
terminal-isolation-gate.sh DEFERRED — fixture-isolation semantics unresolved; tracked as P12 follow-up
rp2-directory-pathspec-gate.sh DEFERRED — RP-2 staged-blob verify too strict for archive; tracked as follow-up
